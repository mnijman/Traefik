http:
  middlewares:
    https-redirect:
      redirectScheme:
        scheme: https
    url-regex:
      redirectRegex:
        regex: "^^https?://(.*).lan/?"
        replacement: "https://${1}.domain.com"
        permanent: true

    add-admin:
      addPrefix:
        prefix: /admin
